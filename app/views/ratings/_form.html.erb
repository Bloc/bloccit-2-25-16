<%= f.label :rating %>
<% if r != nil %>
  <%= f.select :rating, options_for_select(Rating.severities, r.severity) %>
<% else %>
  <%= f.select :rating, options_for_select(Rating.severities) %>
<% end %>